#LESSER OF TWO EVENS:write the function that returns the lesser of two given numbers .if both numbefs are even,but returns the greater if one or both numbers are odd.
def lesser_of_even(a,b):
    if a%2==0 and b%2==0:
        #BOTH NUMBERS ARE EVEN
        if a<b:
            result=a
        else:
            result=b
        else:



